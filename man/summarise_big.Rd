% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_big.R
\name{summarise_big}
\alias{summarise_big}
\title{Summarise a large Arrow dataset by group}
\usage{
summarise_big(
  .data,
  ...,
  .by,
  .order_by = NULL,
  .workers = 2L,
  .strategy = c("parallel_chunks", "shared_chunk", "map_reduce"),
  .chunk_rows = 1e+06,
  .task_rows = 1e+05,
  .oversize = c("error", "warning"),
  .map_reduce = NULL,
  .finalize = NULL,
  .seed = TRUE,
  .try_arrow = TRUE,
  .tmp = tempfile("summarise-big-"),
  .keep_tmp = FALSE
)
}
\arguments{
\item{.data}{An Arrow Dataset or Arrow object supporting dplyr operations.}

\item{...}{Named grouped summary expressions for the ordinary strategies.}

\item{.by}{A single grouping column.}

\item{.order_by}{Optional character vector of columns defining deterministic
order within groups for order-sensitive R summaries.}

\item{.workers}{Number of local Mirai workers. Parallel package tests should
use at most two workers. Defaults to 2.}

\item{.strategy}{One of \code{"parallel_chunks"}, \code{"shared_chunk"}, or
\code{"map_reduce"}.}

\item{.chunk_rows}{Target maximum number of rows per materialized disk chunk.
Complete groups are never split.}

\item{.task_rows}{Target rows per worker task within \code{shared_chunk}. Complete
groups are never split.}

\item{.oversize}{What to do when one group exceeds \code{.chunk_rows}.}

\item{.map_reduce}{Named list of Arrow-computable grouped reduction
expressions, normally supplied as one-sided formulas.}

\item{.finalize}{R function applied to the compact table produced by
\code{.map_reduce}. It must return a data frame.}

\item{.seed}{Passed to \code{futurize::futurize()} for parallel random-number
handling.}

\item{.try_arrow}{If \code{TRUE}, ordinary strategies attempt the complete summary
in Arrow before materializing any raw groups.}

\item{.tmp}{Temporary directory used for group-safe Parquet repartitioning.}

\item{.keep_tmp}{If \code{TRUE}, retain the temporary dataset after completion.}
}
\value{
A data frame containing grouped summary results.
}
\description{
\code{summarise_big()} is designed for grouped computations on Arrow datasets
when the result may be expressible in Arrow, reconstructible from compact
Arrow-computable state, or require arbitrary R code on complete groups.
}
\details{
The ordinary strategies first attempt the complete summary lazily in Arrow.
If Arrow cannot execute it, \code{parallel_chunks} materializes different
group-safe chunks independently, while \code{shared_chunk} materializes one
chunk at a time and can share it between workers with \code{mori}.

With \code{strategy = "map_reduce"}, Arrow computes the expressions supplied in
\code{.map_reduce}, only that reduced table is collected, and \code{.finalize} runs in
ordinary R.
}
\examples{
tab <- arrow::Table$create(data.frame(
  grp = c("a", "a", "b", "b"),
  x = c(1, 2, 10, 20)
))

# Arrow fast path.
summarise_big(tab, result = mean(x), .by = grp)

# Arrow reduction followed by an ordinary R finalizer.
summarise_big(
  tab,
  .by = grp,
  .strategy = "map_reduce",
  .map_reduce = list(n = ~ dplyr::n(), sx = ~ sum(x)),
  .finalize = function(d) {
    dplyr::mutate(d, result = sx / n)
  }
)

\dontrun{
# Arbitrary R function on complete groups in a Parquet Dataset.
ds <- arrow::open_dataset("data")
summarise_big(
  ds,
  result = my_custom_function(x, y),
  .by = grp,
  .strategy = "parallel_chunks",
  .workers = 2
)
}
}
